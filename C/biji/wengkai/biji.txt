const int 用来命名常量，常量名应用大写;



a + b   此表达式运算符前后两个算子，为双目运算符
a*-b    后面的-是负号，优先于乘除计算，因为他是单目运算符
a = b = 6   赋值自右向左运算，先得到b = 6，再有a = b                //应该是



if (条件成立){
    语句
} else {
    语句
}



if ()
    上面没有分号，这里直接写条件也可以，只有一句有效,但若是下一句是if判断，则会运行整个判断，看起来简洁；



C语言中缩进几乎无作用，如果没有大括号，else会与最近的if配对，所以一定要写大括号



else if可理解为
else {
    if
}



优先级： 算术运算符 > 关系运算符 > 赋值运算符
关系运算符优先级： 不等于 > 等于
关系运算从左到右  6 > 5 > 4  结果是0，6 > 5得到1，1 > 4 得到0



switch （控制表达式 （只能是整数） ）{
case 常量 ：                                                       //常量也可以是常量的计算式、赋值为常量的变量，当控制表达式的值等于该常量时，运行下方代码
    语句
    break                                                         //case没有分割这些情况，可看作目录，只是通过控制表达式的数值快速检索到对应情况并运行，知道运行到break才终止
... ...
default :
    语句
    break
}



while (条件){
    循环体
}



do                                                          //与while的区别：while循环前判断，do-while循环后判断，
{
    循环体
} while (条件)；



#include <stdlib.h>                                         //引用函数
#include <time.h>
stand(time(0));         变成srand了，不知为何                 //产生随机数
int a = rand()%101;                                         //取余101可是产生的随机数在 0 到 100 之间



int a, b;
printf("%f", 1.0 * a / b)                                   //使计算结果转变为浮点型



for (i = 1; i <= n; i++) {                                  //for循环计算阶乘   条件判断在循环前    i++也可以改成其他递推式     当不满足循环条件时，不再进入循环，但会再执行一次 i++
    fact *= i;
}



goto 标号(例如out);                                          //上下标号要保持一致，可实现从上方代码快速移动到下方代码，可用于跳出多重循环。
标号：



printf("%.2f", i)                                           //保留两位小数



a = 0;
if (!a) {}                                                  // ！ 为相反， 1 就是在执行



char < short < int < long < long long < float < double < long double
sizeof()                                                    // 空格内可以填 int 等类型，或某一变量，可输出此类型或变量占据几个字节
sizeof(a++)                                                 // 可以输出 a++ 占据的内存，但 a 并没有加一
char a = 255;                                               //输出 a 得到 -1
uunsigned char a = 255;                                     //输出255， unsigned 也可通过在数字后加 U 或 u ，表示将后面的数看作是一个纯二进制数，正数表达范围变大，不能表达负数，可用来扩大正数范围

float b;
b = 1.2345f;                                                // 不在最后加 f / F 会转换 double 类型



char 类型可存储字符串，例如输入 '1' 可输出整型 1 ，也可以通过 %c 输出 1 的ASCLL码 49， 输入ASCLL也使用 %c

a >= 'A' && a <= 'Z' 表示 a 是大写字母

逗号运算符不要忘了

写函数时要注意单一出口，不然之后修改会很麻烦

如果函数不返回值，可以不写return